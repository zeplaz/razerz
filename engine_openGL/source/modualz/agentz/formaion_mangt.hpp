

void update_slot_assignment();

typename std::pair<float,int> fee_et_slot;

struct entity_and_slotz {
//  enity..?
float  assignment_restirction_val;
std::std::vector<fee_et_slot> paired_commitment;
 //entity_oragent and slot?z std::vector<>

for(int i =0; i < slot_assignt; i++)
{
  //run a lamba..
  for (int j_pos =0;j_pos<paired_commitment.size();j_pos++)
  {


 demak_formation_assignie = new doplyment_pos ()
assgint
}a
}


};
